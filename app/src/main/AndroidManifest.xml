<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication21">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".presentaton.activity.StudentsActivity">
            
        </activity>

        <activity android:name=".leetcode.CheckPalindrome">

        </activity>

        <activity android:name=".helpers.classes.SpeechRecognizer">

        </activity>

        <activity android:name=".presentaton.activity.ProgramsActivity">

        </activity>

        <activity android:name=".helpers.classes.Camera">

        </activity>

        <activity android:name=".helpers.classes.SocialNetwork">

        </activity>

        <activity android:name=".presentaton.activity.NotesActivity">

        </activity>

        <activity android:name=".presentaton.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>