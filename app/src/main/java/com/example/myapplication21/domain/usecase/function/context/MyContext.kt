package com.example.myapplication21.domain.usecase.function.context

import android.content.Context

class MyContext {
    fun returnContext(context: Context): Context? {
        return context
    }
}