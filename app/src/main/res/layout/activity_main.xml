<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">


    <ListView
        android:id="@+id/listView_activity_main_productsList"
        android:layout_width="match_parent"
        android:layout_height="270dp"
        android:choiceMode="multipleChoice"
        />

    <Button
        android:id="@+id/button_activity_main_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/activity_main_button_check"
        />
    <ScrollView
        android:id="@+id/scrollView_activity_main_ID"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical"
        android:fillViewport="true">
    <CheckedTextView
        android:id="@+id/checkedTextView_activity_main_checked"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="15sp" />
    </ScrollView>
</LinearLayout>